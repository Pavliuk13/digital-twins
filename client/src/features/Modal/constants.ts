// All possible inner text tokens
export const TEXT = {
  CREATE_BTN: 'Create',
  SAVE_BTN: 'Save',
  DELETE_BTN: 'Delete',
};

// Click events
export const TIMEOUT = {
  CLICK_EVENT_TIMEOUT: 500,
};

// UI Effects
export const DEFAULT_ANIMATION_DURATION = 200; // ms

// Use this config for animation setup or duration config property
// For more info see react-spring api docs
export const DEFAULT_SPRING_ANIMATION_CONFIG = {
  mass: 1,
  tension: 500,
  friction: 45,
};

// Routing
export const GET_PARAMS = {
  modal: 'modal',
};
